package com.hexon.beta;

/**
 * Minimal hand-rolled equivalent of the Android Gradle Plugin's generated
 * BuildConfig — kept around because we build the APK without AGP.
 */
public final class BuildConfig {
    public static final boolean DEBUG = true;
    public static final String APPLICATION_ID = "com.hexon.beta";
    public static final String BUILD_TYPE = "debug";
    public static final int VERSION_CODE = 1;
    public static final String VERSION_NAME = "1.0";
    private BuildConfig() { /* no instances */ }
}
